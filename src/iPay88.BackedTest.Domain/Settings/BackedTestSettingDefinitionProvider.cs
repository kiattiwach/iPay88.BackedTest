using Volo.Abp.Settings;

namespace iPay88.BackedTest.Settings;

public class BackedTestSettingDefinitionProvider : SettingDefinitionProvider
{
    public override void Define(ISettingDefinitionContext context)
    {
        //Define your own settings here. Example:
        //context.Add(new SettingDefinition(BackedTestSettings.MySetting1));
    }
}
